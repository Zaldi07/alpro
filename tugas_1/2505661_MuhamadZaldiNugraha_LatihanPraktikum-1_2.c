#include <stdio.h>
#include <stdlib.h>
int main()
{
    // mendeklarasikan variabel
    int P = 5, L = 7, luas;
    // Proses hitung
    luas = P * L; // Output hasil
    printf("Hasil (Luas) = %d\n", luas);
    return 0;
}